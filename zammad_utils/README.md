# zammad-utils-cc
Zammad API commands for coding challenge

Use the TicketAPI class from `zammad/ticket.py`.

In order to use this class you also need to install python requests module in your python environment.

    pip install requests 